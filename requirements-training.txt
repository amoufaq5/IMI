# IMI Training Requirements (H100 GPU / Cloud GPU)
# Use this instead of requirements.txt when you only need to train, not run the full app.
#
# Usage: pip install -r requirements-training.txt
#
# Training mode: Full fine-tuning on H100 (no LoRA/QLoRA)

# LLM & ML (core training stack)
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0
datasets>=2.16.0
sentencepiece>=0.1.99
safetensors>=0.4.0
einops>=0.7.0
flash-attn>=2.5.0          # Flash Attention 2 for H100

# Training (Mixtral 8x7B full fine-tuning pipeline)
trl>=0.7.0
deepspeed>=0.12.0           # ZeRO Stage 3 for multi-GPU full fine-tuning

# Inference (vLLM)
vllm>=0.3.0

# Data Collection (no credential-based tools needed)
pymupdf>=1.23.0

# Utilities
pyyaml>=6.0
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
